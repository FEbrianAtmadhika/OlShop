import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    height: 20,
    width: 67,
  },
  button: {
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
    width: '33%',
  },
});
